package com.vinod.expense8puzzle.models;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class PuzzleInputTest {

    @Test
    void testValidArgsShouldReturnPuzzleInput() {
        String[] args = {"start.txt", "goal.txt", "DFS"};
//        PuzzleInput input = PuzzleInput.parseArgs(args);

//        assertEquals("start.txt", input.startFile());
//        assertEquals("goal.txt", input.goalState());
//        assertEquals(SearchAlgorithm.DFS, input.searchAlgorithm());

    }
}
